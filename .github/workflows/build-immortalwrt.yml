name: Build ImmortalWrt for aw1000

on:
  workflow_dispatch:
  push:
    branches: [main]

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: master
  DEVICE_TARGET: aw1000
  FILES: files

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
            python3-setuptools rsync swig curl wget file unzip zlib1g-dev \
            libelf-dev libudev-dev cmake ccache

      - name: Clone ImmortalWrt
        run: |
          git clone ${{ env.REPO_URL }} --depth=1 -b ${{ env.REPO_BRANCH }} openwrt
          cd openwrt
          echo "src-git daed https://github.com/QiuSimons/DAED" >> feeds.conf.default

      - name: Update & Install Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure Target & Kernel
        run: |
          cd openwrt
          cat >> .config << 'EOF'
          CONFIG_TARGET_mediatek=y
          CONFIG_TARGET_mediatek_filogic=y
          CONFIG_TARGET_mediatek_filogic_DEVICE_aw1000=y
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_PACKAGE_daed=y
          CONFIG_PACKAGE_ca-bundle=y
          CONFIG_PACKAGE_kmod-sched-bpf=y
          CONFIG_PACKAGE_kmod-sched-core=y
          CONFIG_PACKAGE_kmod-veth=y
          CONFIG_PACKAGE_kmod-xdp-sockets-diag=y
          CONFIG_NETKIT=y
          EOF
          make defconfig

      - name: Download Sources
        run: cd openwrt && make download -j$(nproc)

      - name: Build Toolchain
        run: cd openwrt && make tools/install -j$(nproc) && make toolchain/install -j$(nproc)

      - name: Build Firmware
        run: cd openwrt && make -j$(nproc) || make -j1 V=s

      - name: Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-aw1000
          path: openwrt/bin/targets/*
